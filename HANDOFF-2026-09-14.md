# irfaninvest.com handoff, 2026-09-14 (read this FIRST on SITE-LOAD)

Supersedes every earlier HANDOFF as the starting point. Older ones stay as
history: `HANDOFF-2026-09-10-HEAD-TERM.md` (keyword map, SERP read),
`HANDOFF-2026-09-09-SEO.md` (slug aliases, noindex stubs, price-per-sqm method),
`HANDOFF-2026-08-17.md` (inventory ingest, routing allowlist).
`AGENTS.md` holds the hard rules for any agent (Claude or Codex).

## 0. Live state at handoff

- HEAD `692e587` on `main`, pushed (0 ahead / 0 behind), tree clean, deployed and verified live.
- Deploy is ONLY `vercel deploy --prod --yes` from this folder. `git push` deploys nothing.
- Articles: 196 prerendered pages over 56 slugs (en 51, fa 49, ar 48, ru 48 rows) plus 21 noindex stubs.
- Inventory: 452 available units, 12 projects, entry OMR 61,635, portfolio median OMR 1,063 per m².
- Sitemap: ~668 URLs, well-formed.

## 1. What shipped 2026-09-09 to 09-14

| Commit | What |
|---|---|
| `75c0c66` | Cross-slug hreflang (`src/insightAliases.mjs`); 21 soft-404 language editions now real noindex pages |
| `f0dad21` | /about tells crawlers what the company is |
| `46b9b78` | EN keyword map: `/` = "oman real estate", `/buy` = "buy property in oman"; `/buy` got build-time inventory tables + PAA FAQ |
| `7448095` | 351 unit pages had 23 distinct titles; now 217 (project name + size) |
| `0ada38c` | fa/ar/ru homepage titles restored (EN split only) |
| `91f8c23` | TEMPORARY `RECRAWL_REDIRECTS` in `api/sitemap.js` (3 redirected fa guides). **Remove by 2026-10-31** |
| `926d473` | `AGENTS.md` for Codex |
| `692e587` | **Critical fix, see section 2** |

Content (Supabase `insights`, no commit): Sarooj Apartments (ids 204 to 207),
Villas and houses for sale in Oman `buy-villa-in-oman-2026` (ids 208 to 211, keyword pass done
from Google autocomplete + GSC in all 4 languages), invest-oman-real-estate fa edition + ar/en/ru parity.

## 2. The bug that matters most for SEO recovery

From 2026-08-30 (`ad30b67`) to 2026-09-14 (`692e587`), every H2/H3 in the
prerendered HTML of all articles was `<hundefined>[object Object]</hundefined>`:
the heading renderer used marked's old `(text, level, raw)` signature and marked 18
passes one token object. Browsers looked fine because React re-renders `#root`;
crawlers' first fetch saw no headings for two weeks. Fixed and the build now throws if
it recurs.

**Lesson:** verify SEO changes in the RAW prerendered HTML (curl + parse `<article>`),
never only in a browser or by title/meta/JSON-LD, which come from other code paths.
Expect some article ranking recovery once Google recrawls; watch it.

## 3. Rules learned this week (also in memory)

- Title/keyword changes: one page per intent, and verify ownership first (query `insights.seo_title` + `src/seoRoutes.mjs`) so pages do not cannibalize.
- Evidence from one language does not transfer to another.
- Price per m²: compute price / total_area_sqm, median, never the stored column.
- `leads.created_at` is unreliable; use `inserted_at`.
- Arabic: «عمان» alone means Amman, Jordan in Google. Write «سلطنة عُمان» / «مسقط».
- zsh: never name a shell variable `path`.
- Before claiming "not in Google": read GSC Chart + Pages CSVs first.

## 4. Data sources and their status

| Source | Status |
|---|---|
| GSC export (owner downloads) | Works. Latest: `~/Downloads/irfaninvest.com-Performance-on-Search-2026-09-13/` (3 months to 2026-09-11) |
| Google autocomplete (`suggestqueries.google.com`, curl) | Works, best free keyword signal |
| SEOcrawl connector | Connected but `billing_context_missing` (owner must enable billing and connect GSC/GA4 inside SEOcrawl) |
| Ahrefs connector | `Insufficient plan` (needs API plan) |
| Semrush `execute_report` | Broken (params sent as string) |
| Vercel analytics connector | 403 on scope `amirshamsipurs-projects` (re-authorise) |
| GA4 tables in Supabase | Stale since 2026-07-17 |
| Google SERP in browser | CAPTCHA, do not bypass |

## 5. SEO BOOST PLAN (start here, in order)

Baseline to beat (GSC, 3 months to 2026-09-11): 1,858 clicks, 101,257 impressions,
CTR 1.8%, average position about 6. Daily clicks about 32 (dipped to 21 to 22 on 09-10/11).

### Task 1. CTR rewrite on high-impression pages (Claude, do first)
The site's bottleneck is the snippet, not rank.

| Page | Pos | Impr | CTR |
|---|---:|---:|---:|
| /insights/iranians-abroad-car-import-oman-law-1405 (EN) | 3.9 | 6,557 | 0.21% |
| /insights/oman-bank-loans-mortgage-financing-foreigners-2026 | 5.9 | 7,282 | 1.21% |
| /insights/invest-muscat-stock-exchange-msx-foreigners-2026 | 5.1 | 7,157 | 0.94% |
| /insights/company-formation-cost-oman-2026 | 5.3 | 6,768 | 1.48% |
| /insights/oman-taxes-income-corporate-vat-investors-2026 | 6.8 | 3,100 | 0.39% |
| /insights/oman-100-foreign-ownership-fcil-negative-list-2026 | 4.8 | 1,876 | 0.75% |
| /insights/oman-owner-visa-sponsor-free-2026 | 5.1 | 3,876 | 1.63% |

Steps: get the queries each page ranks for (ask owner for GSC Performance filtered by
page, Queries tab export, or use SEOcrawl once billing works) plus autocomplete; rewrite
`seo_title` (<= 60) and `seo_description` (<= 160) in `insights` so the snippet matches the
dominant query; first-paragraph alignment where needed; no dashes; deploy; verify raw HTML.
Note: the car-import EN title says "Iranians living abroad", likely mismatching English
searchers. Confirm with query data before changing.
Acceptance: re-export GSC 3 to 4 weeks later; these pages' CTR up, position not down.

### Task 2. Striking-distance pages (positions 8 to 20)
«خرید ملک در عمان» 8.1 (fa pillar `kharid-melk-dar-oman-2026` and `/fa/buy`),
`/buy/hawana-salalah` 10.4 (1,588 impr), "company registration in oman" 11.2,
"oman golden visa" 11.9 (`/oman-golden-visa`), `/buy-property-in-salalah` 14.7,
"buy property in oman" 20.3 (`/buy`). Work: content refresh with live data, exact-anchor
internal links from the strongest articles, FAQ matching PAA/autocomplete. Check ownership first.

### Task 3. Recrawl after the heading bug
Owner: Request Indexing for the top 10 to 15 pages (villa article en/fa, fa pillar,
business ideas, company cost, bank loans, MSX, owner visa). Claude: compare article
positions in the next GSC export against this baseline.

### Task 4. Area landing pages (Claude, needs a route + vercel.json rewrite each)
Al Mouj, Sultan Haitham City, Yiti, Jebel Sifah, Salalah. `/project?area=` URLs are not
indexable. Each landing: live price table (build-time, median per m²), unit mix,
projects, FAQ, 4 languages. Check existing articles per area first to avoid cannibalization
(e.g. `al-mouj-muscat-property-prices-guide-2026`, `sultan-haitham-city-property-guide-2026`).

### Task 5. Unit-page consolidation (NEEDS OWNER DECISION first)
196 of 351 indexable unit pages still share a title with a near-identical sibling
(same project, beds, size). Option: canonicalize same project+beds+size units to one page.

### Task 6. Technical
- Homepage mobile LCP about 4.9s (2.1 MB hero video). Needs owner design OK.
- Apex `irfaninvest.com` returns 307, should be 301 to www (Vercel Domains, owner).
- Remove `RECRAWL_REDIRECTS` from `api/sitemap.js` by 2026-10-31.
- `submit-form` edge function drops `geo_country` (redeploy with it in the allowlist).

### Owner-only (biggest long-term ceiling)
1. Claim Google Business Profile for the Muscat office (competitor holds a GBP named for «خرید ملک در عمان»).
2. Backlinks: agent listings on developer sites (Dar Global, Muriya, Sarooj, Al Abrar), Oman Chamber and business directories, Iranian/Russian Gulf community sites.
3. Monthly Oman property price index as a press release (unique data, linkable).
4. Enable SEOcrawl billing / Ahrefs API plan / re-authorise Vercel so monitoring stops depending on screenshots.
5. Rotate the `insights-admin` password (it is in public git history).

### Measurement gaps that hide SEO value
- Website form leads never reach `crm.leads` (`website_lead_id` always null).
- `ai_conversations` has no rows since 2026-07-01 (chat logging dead).
- Last website form lead 2026-09-12 11:36 Muscat; CRM still receiving Meta/Dubizzle leads. Ask owner to submit one test form.
